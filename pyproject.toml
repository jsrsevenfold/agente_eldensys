[project]
name = "eldensys-agent"
version = "0.4.0"
description = "EldenSys local print agent for Windows"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
